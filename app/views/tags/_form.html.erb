<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.text_field :tag_id %>
  </div>
  <div class="field">
    <%= f.label :rssi %><br>
    <%= f.text_field :rssi %>
  </div>
  <div class="field">
    <%= f.label :antenna %><br>
    <%= f.text_field :antenna %>
  </div>
  <div class="field">
    <%= f.label :last_seen_at %><br>
    <%= f.date_select :last_seen_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
